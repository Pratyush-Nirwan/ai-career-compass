# Gemini API key - get one at https://aistudio.google.com/apikey
# Add to server/.env or set as environment variable when starting the server
GEMINI_API_KEY=your_gemini_api_key_here

# Optional: Override API URL from frontend (default uses Vite proxy to localhost:3001)
# VITE_API_URL=http://localhost:3001
